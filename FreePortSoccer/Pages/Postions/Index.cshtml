@page
@model FreePortSoccer.Pages.Postions.IndexModel

@{
    Layout = "_Layout2";
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Position[0].Abbreviation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position[0].PositionDes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Position) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Abbreviation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PositionDes)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PositionId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PositionId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PositionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
